; Repository-level npm config.
; CI and release workflows authenticate with secrets at runtime.
; Do not commit registry tokens, usernames, passwords, or local Verdaccio auth.

registry=https://registry.npmjs.org/
strict-ssl=true
